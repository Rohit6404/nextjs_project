import  styles from "@/app/styles/common.module.css"
const Loading = () => {
    return (
        <section className={styles.loading_section}>
            <div className={styles.loading}>
            </div>
        </section>
    );
};

export default Loading;


// loading.io website ki help se loading animation bana skte h